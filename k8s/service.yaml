apiVersion: serving.knative.dev/v1 
kind: Service 
metadata:
  name: echo 
spec:
  template:
    spec:
      containers:
        - image:  quay.io/monishkumar/microservice-app
          ports:
            - containerPort: 8080 
          env: 
            - name: MY_VAR
              value: "12345"
